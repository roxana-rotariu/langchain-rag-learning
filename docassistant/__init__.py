"""AI Document Assistant — the red-thread application package.

As you work through the lessons (see lessons/), you fill in the modules here:
    config   -> keys, models, settings, prompt/UI text loaders
    ingest   -> load PDF/Office documents and split them into chunks
    store    -> Qdrant: index + retrieval
    rag      -> question -> retrieve -> answer with citations
    agents   -> (stages 4-6) LangGraph, multi-agent, MCP
"""
