"""Agents — filled in during stages 4-6.

Stage 4 (LangGraph): rewrite RAG as a graph with state + tools + explicit flow.
Stage 5 (multi-agent): a supervisor that delegates to sub-agents.
Stage 6 (MCP): expose the RAG tool through an MCP server and consume it in the graph.

Intentionally empty for now — you get here after lessons 1-3.
"""
