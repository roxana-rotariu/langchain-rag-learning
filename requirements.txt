# --- Core LangChain (v0.3+) ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- LLM provider: LOCAL (default) — Ollama + HuggingFace, free, no API key ---
langchain-ollama>=0.2.0
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0     # backend for HuggingFace embeddings

# --- LLM provider: Anthropic (paid fallback) ---
langchain-anthropic>=0.3.0
anthropic>=0.40.0
langchain-voyageai>=0.1.4        # Voyage embeddings (used with Anthropic)

# --- Vector DB: Qdrant ---
langchain-qdrant>=0.2.0
qdrant-client>=1.12.0

# --- LangGraph (stages 4-6) ---
langgraph>=0.2.50
langgraph-checkpoint-sqlite>=2.0.0

# --- MCP (stage 6) ---
mcp>=1.1.0
langchain-mcp-adapters>=0.1.0

# --- Document loaders / parsing ---
pypdf>=5.0.0                  # PDF
docx2txt>=0.8                 # Word (.docx)
openpyxl>=3.1.0               # Excel (.xlsx)
python-pptx>=1.0.0            # PowerPoint (.pptx)
unstructured>=0.16.0          # backend for Excel/PPT loaders
beautifulsoup4>=4.12.0

# --- Web UI ---
streamlit>=1.40.0

# --- Config ---
python-dotenv>=1.0.0        # secrets from .env
pyyaml>=6.0                 # settings / prompts / ui text from config/*.yaml

# --- Utilities ---
rich>=13.0.0

# --- Dev tooling ---
ruff>=0.8.0                 # linter / formatter (config in pyproject.toml)
